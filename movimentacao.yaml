PatchMovimentacao:
  required:
    - valor
  type: object
  properties:
    valor:
      type: number
Movimentacao:
  required:
    - conta
    - valor
  allOf:
    - $ref: "#PatchMovimentacao"
    - type: object
      properties:
        conta:
          $ref: 'conta.yaml#/Conta'
        valor:
          type: number