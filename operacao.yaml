OperacaoCrua:
  type: object
  properties:
    fato:
      $ref: "fato.yaml#/Fato"
    movimentacoes:
      type: array
      items:
        $ref: "movimentacao.yaml#/Movimentacao"
  required:
    - fato
    - movimentacoes
Operacao:
  type: object
  required:
  properties:
    id:
      $ref: "#/OperacaoCrua"
      required: true
    fato: 
      $ref: "fato.yaml#/Fato"
    movimentacoes:
      type: array
      items:
        $ref: "movimentacao.yaml#/Movimentacao"

  
