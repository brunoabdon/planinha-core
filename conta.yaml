Conta:
  required:
  - id
  - nome
  type: object
  properties:
    id:
      $ref: 'types.yaml#/Id'
    nome:
      type: string
PatchConta:
  required:
  - nome
  type: object
  properties:
    nome:
      type: string
