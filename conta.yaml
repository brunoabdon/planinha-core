Conta:
  required:
  - id
  - nome
  type: object
  properties:
    id:
      $ref: 'types.yaml#/Id'
    nome:
      type: string
ContaPatch:
  required:
  - nome
  type: object
  properties:
    nome:
      type: string
