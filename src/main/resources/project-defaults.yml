swarm:
  datasources:
    jdbc-drivers:
      org.postgresql:
        driver-class-name: org.postgresql.Driver
        xa-datasource-class-name: org.postgresql.xa.PGXADataSource
        driver-module-name: org.postgresql
    data-sources:
      gastosoDS:
        driver-name: org.postgresql
        connection-url: ${planinha.database.url}
        user-name: ${planinha.database.user}
        password: ${planinha.database.password}
