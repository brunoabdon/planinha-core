<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.github.brunoabdon.planinha</groupId>
  <artifactId>planinha-core</artifactId>
  <name>Planinha - Core</name>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>war</packaging>

  <properties>
    <version.thorntail>2.4.0.Final</version.thorntail>
    <version.postgresql>42.2.5</version.postgresql>
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
    <failOnMissingWebXml>false</failOnMissingWebXml>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    
    <herokuds.database_url>${env.DATABASE_URL}</herokuds.database_url>
    
    <abd-commons.version>2.0.0-SNAPSHOT</abd-commons.version>
    <gastoso.version>2.0.0-SNAPSHOT</gastoso.version>
    
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.thorntail</groupId>
        <artifactId>bom-all</artifactId>
        <version>${version.thorntail}</version>
        <scope>import</scope>
        <type>pom</type>
      </dependency>
    </dependencies>
  </dependencyManagement>

 <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
      </resource>
    </resources>

    <plugins>
      <plugin>
        <groupId>com.github.brunoabdon.m2.herokuds</groupId>
        <artifactId>herokuds-maven-plugin</artifactId>
        <version>0.0.3</version>
        <executions>
          <execution>
            <phase>validate</phase>
            <goals>
              <goal>parse</goal>
            </goals>
          </execution>
        </executions>
      </plugin> 
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>2.6</version>
        <configuration>
          <failOnMissingWebXml>false</failOnMissingWebXml>
        </configuration>
      </plugin>
      <plugin>
        <groupId>io.thorntail</groupId>
        <artifactId>thorntail-maven-plugin</artifactId>
        <version>${version.thorntail}</version>

         <executions>
            <execution>
              <id>package</id>
              <goals>
                <goal>package</goal>
              </goals>
            </execution>
          </executions>
          <configuration>
                <!--  
                    The artifact will be named the same as the primary project 
                    artifact (according to ${project.finalName}), unless a 
                    plugin configuration of <finalName>…​</finalName> or a 
                    property of thorntail.finalName is provided.
                    
                    https://docs.thorntail.io/4.0.0-SNAPSHOT/#_other_configuration
                    
                 -->
                <finalName>${project.artifactId}</finalName>
            </configuration>
      </plugin>
    </plugins>
    <pluginManagement>
        <plugins>
            <!--This plugin's configuration is used to store Eclipse m2e settings only. It has no influence on the Maven build itself.-->
            <plugin>
                <groupId>org.eclipse.m2e</groupId>
                <artifactId>lifecycle-mapping</artifactId>
                <version>1.0.0</version>
                <configuration>
                    <lifecycleMappingMetadata>
                        <pluginExecutions>
                            <pluginExecution>
                                <pluginExecutionFilter>
                                    <groupId>
                                        com.github.brunoabdon.m2.herokuds
                                    </groupId>
                                    <artifactId>
                                        herokuds-maven-plugin
                                    </artifactId>
                                    <versionRange>
                                        [0.0.1-SNAPSHOT,)
                                    </versionRange>
                                    <goals>
                                        <goal>parse</goal>
                                    </goals>
                                </pluginExecutionFilter>
                                <action>
                                    <ignore></ignore>
                                </action>
                            </pluginExecution>
                        </pluginExecutions>
                    </lifecycleMappingMetadata>
                </configuration>
            </plugin>
        </plugins>
    </pluginManagement>
 </build>
  
  <dependencies>
    
    <dependency>
      <groupId>io.thorntail</groupId>
      <artifactId>jaxrs</artifactId>
    </dependency>
    
    <dependency>
      <groupId>io.thorntail</groupId>
      <artifactId>cdi</artifactId>
    </dependency>

    <dependency>
      <groupId>io.thorntail</groupId>
      <artifactId>jpa</artifactId>
    </dependency>

    <dependency>
      <groupId>io.thorntail</groupId>
      <artifactId>ejb</artifactId>
    </dependency>

    <dependency>
      <groupId>io.thorntail</groupId>
      <artifactId>logging</artifactId>
    </dependency>

    <dependency>
      <groupId>io.thorntail</groupId>
      <artifactId>transactions</artifactId>
    </dependency>

	<dependency>
	    <groupId>org.postgresql</groupId>
	    <artifactId>postgresql</artifactId>
	    <version>${version.postgresql}</version>
	</dependency>
	
	<dependency>
        <groupId>com.github.brunoabdon.gastoso</groupId>
        <artifactId>gastoso-core</artifactId>
        <version>${gastoso.version}</version>
    </dependency>
    <dependency>
         <groupId>com.github.brunoabdon.gastoso</groupId>
         <artifactId>gastoso-dal</artifactId>
         <version>${gastoso.version}</version>
    </dependency>
    <dependency>
         <groupId>com.github.brunoabdon.commons</groupId>
         <artifactId>abd-commons-rest</artifactId>
         <version>${abd-commons.version}</version>
     </dependency>
     <dependency>
         <groupId>com.github.brunoabdon.commons</groupId>
         <artifactId>abd-commons-util</artifactId>
         <version>${abd-commons.version}</version>
     </dependency>
     <dependency>
         <groupId>com.github.brunoabdon.commons</groupId>
         <artifactId>abd-commons-dal</artifactId>
         <version>${abd-commons.version}</version>
     </dependency>
  </dependencies>
</project>
